import { RecentViewStore } from './recent-view.store';

describe('RecentViewStore', () => {
  // let store: RecentViewStore;
  let store: RecentViewStore;

  beforeEach(() => {
    // store = new RecentViewStore();
    store = new RecentViewStore();
  });

  it('should create an instance', () => {
    // expect(store).toBeTruthy();
  });
});
