import { SortMessagesPipe } from './sort-messages.pipe';

describe('SortMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
