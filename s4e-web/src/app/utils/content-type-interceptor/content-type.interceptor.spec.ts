import {ContentTypeInterceptor} from './content-type.interceptor';

describe('ContentTypeInterceptor', function () {
  const interceptor = new ContentTypeInterceptor();

  it('should set responseType to "json"', () => {
    // :TODO add test
  });
});
